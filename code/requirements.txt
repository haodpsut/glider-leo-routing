# Core scientific stack
numpy>=1.24
scipy>=1.10
pyyaml>=6.0

# Deep learning (CUDA build resolved at install time on the RTX 4090 host)
torch>=2.1

# Plotting for figure generation (paper/figs)
matplotlib>=3.7

# Testing
pytest>=7.4
